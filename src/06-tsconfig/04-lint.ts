// Дополнительный линтинг в конфиге
// по умолчанию все опции отключены

// "noPropertyAccessFromIndexSignature": true, // не позволяет обращаться к необъявленым типам

// "noUncheckedIndexedAccess": true, // обязует делать проверку на то, что поле существует

// "noImplicitReturns": true, // нет неявному ретерну

// "noFallthroughCasesInSwitch": true, // обязует в свиче указывать break

// "noUnusedLocals": true, // обязует использовать все объявленные локальные переменные

// "noUnusedParameters": true, // обязует использовать все объявленные параметры в функции
